#include <stdio.h>
// area of rectangle with any l and b
int main()
{
    int length, breadth;
    /*printf("What is the length of rectangle\n");
    scanf("%d", &length);

    printf("What is the breadth of rectangle\n");
    scanf("%d", &breadth);

    printf("The area of the given rectangle is %d", length * breadth);*/
    int a, b;
    float c;
    a = 5;
    b = 2;
    c = a / b;
    printf("%f", c);
    return 0;
}
