//area of circle with specified radius
#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.14;
    printf("The area of the given circle is %f\n", pi * radius * radius);
    return 0;
}