#include <stdio.h>
// area of rectangle with specified l and b
int main()
{
    int length = 2, breadth = 4;
    int area = length * breadth;
    printf("area of the given rectangle is %d\n", area);
    return 0;
}